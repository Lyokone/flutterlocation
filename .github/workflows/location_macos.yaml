name: location_macos

on:
  pull_request:
    paths:
      - ".github/workflows/location_macos.yaml"
      - "src/location_macos/**"
  push:
    branches:
      - main
    paths:
      - ".github/workflows/location_macos.yaml"
      - "src/location_macos/**"

jobs:
  build:
    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/flutter_package.yml@v1
    with:
      flutter_channel: stable
      flutter_version: 3.0.0
      working_directory: src/location_macos
