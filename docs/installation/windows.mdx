---
title: Install on Windows
---

# Install on Windows

Windows is working out of the box! 🎉

The plugin uses the `Windows.Devices.Geolocation` APIs, which prompt the user
for location access on first use. Make sure Location is enabled in the
Windows privacy settings.
