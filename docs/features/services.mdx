---
title: Services
description: Check service status
---

# Services

Services refers to the options available to you in order to get the location of your user.

On **Android** you can have the status of the GPS and Network services.
The Network service is often less precise.

On **iOS** both the GPS and the Network service are available but cannot be requested individually. Either the Location Service is activated or not.

```dart
Future<bool> isGPSEnabled()
```

```dart
Future<bool> isNetworkEnabled()
```

If the service are disabled, you can make a call to `getLocation` or `onLocationChanged`.
The call will automatically try to activate the required location service depending on the precision request for the location.

<nav
  aria-label="Docs pages navigation"
  style={{ display: "flex", justifyContent: "space-between", marginTop: "3rem" }}
>
  <a
    href="/Lyokone/flutterlocation/features/permissions"
    style={{
      border: "1px solid #ccd0d5",
      borderRadius: "0.4rem",
      display: "block",
      height: "100%",
      lineHeight: "1.25",
      padding: "1rem",
      transition: "border-color 200ms cubic-bezier(0.08,0.52,0.52,1)",
              textDecoration: "none",
    }}
  >
    <div 
    style={{
        color: "#525860",
        fontSize: "0.875rem",
    }}>Previous</div>
     <div 
    style={{
        color: "var(--theme-color)",
    }}>« Permissions</div>

  </a>
  <a
    href="/Lyokone/flutterlocation/features/notification"
    style={{
      border: "1px solid #ccd0d5",
      borderRadius: "0.4rem",
      display: "block",
      height: "100%",
      lineHeight: "1.25",
      padding: "1rem",
      transition: "border-color 200ms cubic-bezier(0.08,0.52,0.52,1)",
      textDecoration: "none",
      textAlign: "end",
    }}
>
    <div  style={{
        color: "#525860",
        fontSize: "0.875rem",
    }}>Next</div>
    <div  style={{
        color: "var(--theme-color)",
    }}>Notification »</div>

  </a>
</nav>
